# New Attitude Cycle tool

1. Copy this folder to `tools/your-tool-name/`.
2. Add `index.html` (and `css/`, `js/` if needed).
3. Add a card and nav link on the site root `index.html`.
4. Include site nav: link home `../../index.html` and sibling tools under `../`.
