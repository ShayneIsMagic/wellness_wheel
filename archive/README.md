# Archive

`harbor-compass-standalone.html` is a legacy **single-file** build (inline CSS and JavaScript). The live app uses `index.html` with `css/main.css` and `js/main.js` at the project root. Keep this file only for reference or offline sharing; do not deploy it as the primary site.
